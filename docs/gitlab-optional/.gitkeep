# GitLab CI/CD - Optional Configuration

This folder contains GitLab-specific CI/CD configuration files.
These are optional and only needed if you want to use GitLab instead of or in addition to GitHub Actions.
